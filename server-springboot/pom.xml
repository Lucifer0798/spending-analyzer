<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.spendinganalyzer</groupId>
  <artifactId>spending-analyzer</artifactId>
  <version>1.0.0</version>
  <name>spending-analyzer</name>
  <description>Spending analyzer backend (Spring Boot)</description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.53.2.1</version>
    </dependency>

    <!-- Versioned schema migrations. Versions are managed by the Spring Boot parent. -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <!--
      Spring Boot 4 splits auto-configuration into per-technology modules. Without this,
      flyway-core sits on the classpath but is never auto-configured, so migrations are
      silently skipped and the schema is never created.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-flyway</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-csv</artifactId>
      <version>1.14.1</version>
    </dependency>

    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi-ooxml</artifactId>
      <version>5.5.1</version>
    </dependency>

    <dependency>
      <groupId>com.anthropic</groupId>
      <artifactId>anthropic-java</artifactId>
      <version>2.57.0</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      MockMvc. Same Spring Boot 4 split as the Flyway module above: @AutoConfigureMockMvc moved
      out of spring-boot-test-autoconfigure into this per-technology module, so the starter
      alone no longer provides it.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>spending-analyzer</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Builds the React client and packages it into the jar, so `java -jar` serves the whole
      app on one port. Off by default: the everyday `./mvnw verify` loop and the CI server
      job have no use for the frontend, and installing Node adds ~30s to a build that
      otherwise takes seconds. Turn it on for a release build:

        ./mvnw -Pfrontend clean package

      The Docker image build uses this same profile, so there is one definition of how the
      frontend gets built rather than two that can drift.
    -->
    <profile>
      <id>frontend</id>
      <properties>
        <!-- Vite 8 requires ^20.19 || >=22.12, so this floor is not arbitrary. -->
        <node.version>v22.23.2</node.version>
        <client.dir>${project.basedir}/../client</client.dir>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>com.github.eirslett</groupId>
            <artifactId>frontend-maven-plugin</artifactId>
            <version>1.15.1</version>
            <configuration>
              <workingDirectory>${client.dir}</workingDirectory>
              <!-- Keep the downloaded Node out of the client tree; target/ is already ignored. -->
              <installDirectory>${project.build.directory}</installDirectory>
            </configuration>
            <executions>
              <execution>
                <id>install-node-and-npm</id>
                <goals><goal>install-node-and-npm</goal></goals>
                <configuration>
                  <nodeVersion>${node.version}</nodeVersion>
                </configuration>
              </execution>
              <execution>
                <id>npm-ci</id>
                <goals><goal>npm</goal></goals>
                <configuration>
                  <arguments>ci</arguments>
                </configuration>
              </execution>
              <execution>
                <id>npm-build</id>
                <goals><goal>npm</goal></goals>
                <configuration>
                  <arguments>run build</arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!--
            Spring Boot serves classpath:/static as the site root, so index.html becomes the
            welcome page and the hashed assets sit alongside it. The API lives under /api,
            so nothing collides.
          -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-client-dist</id>
                <phase>prepare-package</phase>
                <goals><goal>copy-resources</goal></goals>
                <configuration>
                  <outputDirectory>${project.build.outputDirectory}/static</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${client.dir}/dist</directory>
                      <filtering>false</filtering>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
