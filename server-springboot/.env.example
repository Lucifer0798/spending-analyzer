ANTHROPIC_API_KEY=your-api-key-here
ANTHROPIC_MODEL=claude-opus-5

# Guards this instance. Optional for a local run (which is open by default and says so on
# startup); required by the Docker image, which refuses to start without it.
APP_PASSWORD=
